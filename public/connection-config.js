// Optional public PeerJS/ICE configuration. Never put private credentials here.
window.SCRABBLE_CONNECTION = {
  // The app supplies public STUN + PeerJS TURN fallbacks when this is omitted.
  // Add a private TURN server here for restrictive networks if needed.
  // peerServer: { host: 'your-peer-server.example', port: 443, path: '/', secure: true },
};
